List<Case> caselist = new List<Case>();
for(integer i =1;i<4;i++){
    Case c = new Case();
    c.Status ='New';
    c.Origin = 'Web';
    caselist.add(c);
}
insert caselist;

//yukardakiler nasil update edilir
